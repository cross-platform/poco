import glob
import platform
import sys

config = sys.argv[1]
component = sys.argv[2]

if platform.system() == 'Windows':
	for i in glob.glob('./win/' + config + '/Poco/' + component + '/*.obj'):
		print(i)
else:
	for i in glob.glob('./mac/' + config + '/Poco/' + component + '/*.o'):
		print(i)
